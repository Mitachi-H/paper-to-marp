[project]
name = "paper-to-slides"
version = "0.1.0"
description = "学術論文 (PDF) からプレゼン資料 (pptx) を生成するワークフローツール"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "docling>=2.63.0,<3.0.0",
    "pymupdf",
]

[project.optional-dependencies]
dev = [
    "ruff",
]
